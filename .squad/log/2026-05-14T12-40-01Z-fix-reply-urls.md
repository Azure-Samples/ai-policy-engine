# Session Log — Fix Reply URLs (2026-05-14T12:40:01Z)

**Agent:** Sydnor (Infra/DevOps)  
**Task:** Fix AADSTS500113 redirect URI issue  
**Status:** ✅ FIXED

## Summary

Postprovision scripts were registering redirect URIs on the wrong app registration. Fixed both `postprovision.ps1` and `postprovision.sh` to use the Terraform-managed app ID (`api_app_id`) instead of the legacy app ID (`CONTAINER_APP_CLIENT_ID`). Verified redirect URI is now on the correct app.

**Awaiting:** Zack login retry confirmation.
